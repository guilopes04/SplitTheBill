<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SplitTheBill" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Cor primária do aplicativo (verde) -->
        <item name="colorPrimary">@color/verde</item>
        <item name="colorPrimaryVariant">@color/verde_escuro</item>
        <item name="colorOnPrimary">@color/branco</item>
        <!-- Cor secundária do aplicativo (preto) -->
        <item name="colorSecondary">@color/preto</item>
        <item name="colorSecondaryVariant">@color/preto_escuro</item>
        <item name="colorOnSecondary">@color/branco</item>
        <!-- Cor da barra de status -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Personalize seu tema aqui. -->
    </style>
</resources>
